#include <stdio.h>  

int main() {

  // int a = 10, b = 5;

  int a = 0;

  // if ( a > 0 && b > 0 ) {
  //   printf("Ambos são positivos\n");
  // } else {
  //   printf("Pelo menos um não é positivo\n");
  // }

  // if ( a > 0 || b > 0 ) {
  //   printf("Pelo menos um é positivo\n");
  // } else {
  //   printf("Ambos são negativos\n");
  // }

  if(!a) {
    printf("A é zero\n");
  } else {
    printf("A não é zero\n");
  }
}
